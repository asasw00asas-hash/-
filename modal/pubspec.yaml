name: whats_sender_pro
description: A high-end WhatsApp marketing and messaging tool.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.1.5
  http: ^1.6.0
  device_info_plus: ^13.1.0
  shared_preferences: ^2.5.5
  file_picker: ^11.0.2
  csv: ^8.0.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
